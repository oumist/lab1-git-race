version: 0.1

phases:
  build:
    commands:
       - echo Build started on `date`
       - ./gradlew build
  post_build:
    commands:
       - echo Build completed on `date`
       - zip -r -D lab1-git-race.zip 'build/libs/*.war'
artifacts:
  files:
    - 'lab1-git-race.zip'
  discard-paths: yes
